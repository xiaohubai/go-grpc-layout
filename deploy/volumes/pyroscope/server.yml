scrape-configs:
  - job-name: pyroscope
    scrape-interval: 60s
    enabled-profiles: [cpu, mem, goroutines, mutex, block]
    static-configs:
      - application: my-pyroscope
        targets:
          - 127.0.0.1:8000
        labels:
          env: dev
